import React from 'react';

import ProfileContent from '~/layout/containers/profileContent';

type Props = {};

const Profile: React.FC<Props> = () => {
  return (
    <React.Fragment>
      <ProfileContent />
    </React.Fragment>
  );
};

export default Profile;
